server:
  port: 8501

spring:
  profiles:
    active: @profile.id@
  application:
    name: consul-client-1
  clound:
    consul:
      host: localhost
      port: 8500
    discovery:
      serviceName: ${spring.application.name}

management:
  security:
    enabled: false